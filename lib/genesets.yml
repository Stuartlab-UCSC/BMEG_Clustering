# edges going from geneset to genes
- label: geneset
  gid: "geneset:{{setsName}}:{{genesetName}}"
  vertexes:
    - label: geneset
      merge: True
      filter:
        - genes

  edges:
    - index: genes
      fromLabel: geneset
      toLabel: gene
      label: genesetGene
      from: "geneset:{{setsName}}:{{genesetName}}"
      to: "gene:{{_index}}"
